import { RedisLockService } from './src/infrastructure/redis/RedisLockService'; import { redisClient } from './src/infrastructure/redis/RedisClient'; async function main() { console.log('--- STARTING CONCURRENCY TEST ---'); const lockService = new RedisLockService(); const resource = 'slot:test_concurrency_slot'; try { const p1 = lockService.acquire(resource, 5); const p2 = lockService.acquire(resource, 5); const [lock1, lock2] = await Promise.all([p1, p2]); console.log('Lock 1 acquired:', !!lock1); console.log('Lock 2 acquired:', !!lock2); if (lock1 && lock2) { console.log('FAIL: Both locks acquired!'); } else if (lock1 || lock2) { console.log('PASS: Only one lock acquired!'); } else { console.log('FAIL: Neither lock acquired!'); } if (lock1) await lockService.release(resource, lock1); if (lock2) await lockService.release(resource, lock2); } catch (error) { console.error('Test error', error); } await redisClient.quit(); process.exit(0); } main();
